<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="repeat_timer_maui.MainPage"
             Title="Repeat Timer">
    <ScrollView>
        <VerticalStackLayout
            Padding="24"
            Spacing="16">
            <Label
                Text="Repeat Timer"
                FontSize="28"
                FontAttributes="Bold" />

            <Label
                Text="Duration (seconds)"
                FontSize="16" />

            <Entry
                x:Name="DurationEntry"
                Keyboard="Numeric"
                Placeholder="Enter seconds" />

            <Button
                x:Name="StartButton"
                Text="Start" />

            <Button
                x:Name="NextButton"
                Text="Next" />

            <Button
                x:Name="IncreaseButton"
                Text="Increase 10%" />

            <Button
                x:Name="DecreaseButton"
                Text="Decrease 10%" />

            <Button
                x:Name="DoneButton"
                Text="Done" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
